# Build output
build/bin/
frontend/dist/
frontend/node_modules/

# Upstream reference clone — a separate repo with its own history.
# CLAUDE.md is explicit: reference it, don't vendor it.
open-oscar-server/

# Generated bundle (rebuilt from benchat-tls/ sources)
benchat-tls.tar.gz

# Working notes kept out of source control: deployment specifics (server
# address, VPS details, test-account notes) and security findings that are
# still open. Neither is something to publish — an unfixed finding least of all.
*.local.md

# Never track a database or its backups
*.sqlite
*.sqlite.backup-*

# Local crash/debug captures
bencchat.txt

# Local Claude Code settings
.claude/settings.local.json
